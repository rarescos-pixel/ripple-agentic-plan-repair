# Prior Work run — unreconciled

A prior ChatGPT Work run operated for roughly 32 minutes from an earlier Ripple seed under “Construiește și validează MVP-ul complet”. Its workspace output is still inaccessible from this conversation, GitHub and the indexed Library.

## Canonical rule
- do not assume the Work run produced nothing;
- do not depend on unverified Work state;
- **current canonical state is the public, inspectable Ripple v1.2 repository + Railway deployment + remote smoke evidence**;
- if the old Work workspace becomes accessible later at acceptable cost, reconcile by delta (`KEEP / MERGE / DROP`) and never overwrite verified v1.2 blindly.

The old Work run is not a blocker.
