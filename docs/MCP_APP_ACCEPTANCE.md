# MCP App acceptance criteria

Merge only if CI is fully green and the following invariants hold: exact `ui://` binding, exact MCP App MIME, authenticated resource read, display-only widget, zero writes before approval, no change to approval/execution semantics, and no generated-evidence drift.
